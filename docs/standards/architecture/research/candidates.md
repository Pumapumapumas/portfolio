# Action candidates — the running list

The durable home for research action candidates. `synthesis.md` is rewritten every cycle; **this is not**. A rejected candidate stays visible so it is not re-proposed.

## The rule

> **Research creates and appends. Planning dispositions.**

- Research adds candidates with a **stable ID** (`C-NNN`), never reused, never renumbered.
- Two orthogonal flags — **do not collapse them**:

| Flag | Values | Who sets it |
|---|---|---|
| `decision` | `ship` · `requires review` · `reject` · **blank = not yet triaged** | planning triage |
| `status` | `open` · `closed` | a later process (the item lands in a plan / the build completes it) |

- **Nobody deletes a row.** Both flags carry their reasoning in the Note.

Full mechanics: CDF memory model — `claude-dot-files/docs/guide/memory-model.md`.

## Candidates

**All rows below are from research cycle 1 (2026-08-10), whose five papers have since been verified: each carries `Critic: PASS-WITH-FIXES — 2026-08-10` after an independent critic pass of four correction rounds, and each ended that pass at PASS.** `PASS-WITH-FIXES` records that defects *were* found and repaired — it is a stronger verdict than an unexamined PASS, not a weaker one. Triage should weigh two things: **(a)** the corrections changed several load-bearing numbers, and the affected rows below carry the corrected figure with its criterion — where a Note states a count, that count is the enumerated one; **(b)** one premise did **not** survive verification. *"Two real businesses run production workloads on the platform"* is an **operator-supplied dispatch premise, not a pool finding**, and the workspace's own current-state docs qualify it — see `synthesis.md` §1.4. **C-008, C-009 and C-018 are conditional on the operator establishing that fact.**

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-001 | **Correct the degree on the live site — two places, two different strings.** `index.html` **L16** (hero tagline) carries `biomedical informatics (M.S.)`; **L35** (About) carries `an M.S. in biomedical informatics.` The correct degree is **M.S. Computer Science**. | [`synthesis.md`](synthesis.md) §3 Tier 0.1 | | `open` | **Three of the five cycle-1 analysts flagged this independently, unprompted** — corrected from "all five", which nothing had checked. Enumerated by grepping all five papers for `biomedical` / `M.S.` / `degree` / `computer science`: `audience` §6.1 A7, `portfolio-structure` §0, `self-built-system` §10.5 C1. The case-study and design papers do not mention it — neither audits page copy. It is the most checkable claim on the page; an error there discredits the unverifiable ones (Stanford web-credibility guideline 10: *"Avoid errors of all types, no matter how small they seem"*). **This is NOT a single string replacement**, and the loose "both say" phrasing (signal §10.5 C1, and the structure paper's own "the string occurs twice") hides that: `grep -c biomedical index.html` returns **2** while `grep -c -F 'biomedical informatics (M.S.)' index.html` returns **1**. The two sites carry **different characters** — the hero's parenthetical form and the About's `an M.S. in biomedical informatics.` — so a fix keyed on the hero wording leaves the wrong degree in the About, on the page whose whole Tier-0 rationale is "the most checkable claim on the page". The structure paper (§0) and the audience paper (§6.1 A7) both spell out both strings; use those. **Content is operator-owned per `CLAUDE.md` — research surfaced it and did not edit it.** |
| C-002 | **Remove the 3 dead `href="#"` links and the "Add your next project" placeholder tile** — ship the write-up or drop the affordance, never both-and. | [`raw/audience-calibration-and-anti-patterns.md`](raw/audience-calibration-and-anti-patterns.md) §6.1 A1, P1 | | `open` | Highest-confidence, lowest-cost item in the pool. Enumerated in-repo: `grep -c 'href: "#"' projects.js` → 3; 4 tiles of which 1 is the literal placeholder publishing the site's own build instructions as content. Fogg's prominence qualifier makes it worse, not better — these sit on every tile's primary CTA. |
| C-003 | **Close the two live conformance defects** — `--border #262a35` at 1.34:1 is the sole boundary identifying the hero pill controls (WCAG 1.4.11 needs 3:1); `scroll-behavior: smooth` ships unconditionally with zero `prefers-reduced-motion` queries. | [`raw/design-language-2026.md`](raw/design-language-2026.md) §10.2, §10.4 | | `open` | Both ratios computed from the live `style.css` by the WCAG formula, not asserted. A site whose thesis is "I build infrastructure correctly" cannot ship a conformance defect. |
| C-004 | **Run the measurement + recall backlog before writing any case study** — availability over a stated window with the instrument named; an SLO for one user-facing service per business; production start date; provisioning and rebuild-drill timings; one real incident with dates and consequence; 2–4 decisions with alternatives actually rejected. | [`raw/self-built-system-as-senior-signal.md`](raw/self-built-system-as-senior-signal.md) §10.6; [`raw/case-study-craft-for-systems-work.md`](raw/case-study-craft-for-systems-work.md) §8.5, B1 | | `open` | **The strongest sections of the recommended case study need numbers that do not exist yet.** Both papers left every numeric slot as an explicit `MEASURE:`/`RECALL:` placeholder and attributed **no measured quantity** to this platform. **Corrected scope — the earlier "no number" here was the pre-correction breadth the case-study paper was itself narrowed away from.** Its Read-this-first block now reads *"no measured quantity — no timing, no uptime, no throughput, no enumerated inventory — is attributed to the MDC platform anywhere in this paper"*, and immediately carves out the two **non-measured structural descriptors** it does carry — *"three K3s clusters", and the two businesses the platform is built to serve* — both taken from the operator's own in-repo content and cited there, with their actual current build status stated (§8.1). "Three K3s clusters" is a number; the distinction that matters is measured-vs-structural, not numeric-vs-not. **See C-018:** that carve-out is exactly the claim now live on the site. The first build task is measurement, not writing. |
| C-005 | **Adopt the 8-section information architecture** (hero → proof strip → flagship system → selected work → write-ups → About → contact → footer), replacing today's hero → About → 3 tiles → footer. | [`raw/portfolio-structure-and-content.md`](raw/portfolio-structure-and-content.md) §4.6 | | `open` | The entire section list is **derived**, and no controlled study of personal engineering portfolios was found to exist — searched via targeted academic search, URL-probing of NN/g's article namespace, and Crossref/Semantic Scholar (§5.1, method stated). Treat as a proposal to be tested. **Corrected population — the earlier note here said NN/g's findings were collected on "corporate/e-commerce/B2B sites", which the paper was itself corrected for.** §5.1 now reads *"Every NN/g finding in this paper was collected on corporate, e-commerce, B2B, news, blog, or encyclopedic sites"*, quoting the scrolling study's own list: *"including news, ecommerce, blogs, FAQs, and encyclopedic pages."* **Blogs are in that population and 15 of the 21 sites are blogs by structure, so the scrolling finding crosses no genre boundary at all.** The gap **narrows without closing**: the study reports no blog-only breakdown, so how much of the 57%/74% distribution came from blog pages is unrecoverable, and the other NN/g findings this list leans on — homepage principles, B2B, About-Us — carry **no** blog population. The one-person-portfolio transfer remains inference. |
| C-006 | **Adopt the 10-section case-study template** — title / at-a-glance / problem+constraints / two diagrams / 3–5 decision blocks with rejected alternatives and costs / evidence / one incident / trade-offs / dated status / artifacts. Target 1,200–2,000 words. | [`raw/case-study-craft-for-systems-work.md`](raw/case-study-craft-for-systems-work.md) §8 | | `open` | Word budget is derived from a 2008 NN/g cost model, not measured (G4). Every exemplar behind the template is an *organisation* writing about production systems at scale — **all seven, enumerated from B3: Cloudflare, GitLab, Discord, Segment, Google, Oxide, Tailscale** — each importing institutional credibility the reader has before the first sentence. The transfer to a sole operator is inference (no source was located that studies it — §11 G5), and the sections that read as rigour from a company may read as over-formality from an individual (B3). |
| C-007 | **Ship the MDC case study first** and make at least one `Write-up →` link resolve. | [`synthesis.md`](synthesis.md) §3 Tier 3 | | `open` | Blocked on C-004. Sequencing note: *Declarative VM Orchestrator* is the study whose evidence section is naturally strongest; *Deploy-A-Saurus* is where the decisions section is — and this repo's own `verify → build` gate is a live third-party-attested artifact for its artifacts section. |
| C-008 | **Rewrite hero + About into objective register**, carrying the thesis line *"I design, build, and operate production infrastructure end to end — and two real businesses run on the platform I built."* Delete *enterprise-grade*, *from scratch*, *whole systems not just pieces*. | [`raw/self-built-system-as-senior-signal.md`](raw/self-built-system-as-senior-signal.md) §10.1–10.3; [`raw/audience-calibration-and-anti-patterns.md`](raw/audience-calibration-and-anti-patterns.md) §6.1 A3, §7 item 2 | | `open` | NN/g's measured rewrite of promotional copy into plain fact scored **+27% alone (§6.1 A3)** and **+124% combined with concision and scannability (§7 item 2)** — the two figures sit at different sections; an earlier note here cited both to A3. The words to cut trace to different rulings too: *enterprise-grade* to audience C7 and signal §10.5 C2, *from scratch* to signal §11 and §10.5 C3, *whole systems, not just pieces* to audience A3/A14 — audience C7's own cut list is *enterprise-grade, robust, cutting-edge*. Includes the self-named boundary sentence ("one operator — not evidence of leading a team; evidence of owning a system end to end, including the pager"), which the paper argues converts the weakest point into the most credible sentence on the page. **Split this row when triaging: the objective-register rewrite is unconditional and can ship as soon as the operator rules; the thesis line's second clause ("two real businesses run on the platform I built") is NOT established by this pool** — it is a dispatch premise the workspace's own current-state docs qualify (`synthesis.md` §1.4), and publishing an unchecked checkable claim is the inflation the case-study paper's §6.3 warns against. **Blocked with C-009 on establishing the fact.** |
| C-009 | **State the production-dependency fact plainly** — what runs on the platform, for whom, since when. | [`raw/audience-calibration-and-anti-patterns.md`](raw/audience-calibration-and-anti-patterns.md) §8, P5 | | `open` | The pool's single highest-value unused sentence, and the site currently says only "the businesses I run on top of my own infrastructure" — passive, unnamed, unquantified, reading as hobby framing. **BLOCKED ON TWO THINGS, not one.** (1) C-004, for the "since when". (2) **Establishing the fact itself** — no paper in this pool established that two businesses run production workloads on the platform; it entered as a dispatch premise (`audience` §8 marks the whole section derived, inputs including *"the dispatch's description of the subject"*), and the case-study paper was corrected at four sites for asserting it, its §8.1 now reading *"Do not fill the Status slot with "in production; two businesses run on it" — the workspace's own current-state docs contradict both halves"*. Qualifying evidence, re-verified 2026-08-10: `/opt/skyy-net/CLAUDE.md` §"Three K3s Clusters **(planned)**" with the control plane on the "temporary **k3s-0** bootstrap cluster … today"; `algorithm-and-sons/CLAUDE.md` "**Repo status:** founding/planning phase"; its `roadmap.md` "**Founding DRAFT — not yet ratified.**" with 1 checked box against 20 unchecked (enumerated); `phase1_deploy.md` L47 "P1 otherwise runs testing-only (synthetic data) until it lands" and L127 "Rue onboards in P2". **Research is not ruling on the operator's businesses — only recording that the pool never established this and the in-repo evidence currently qualifies it.** Until it is established, the shippable form is the platform-side dated status sentence per `case-study` §8.1. |
| C-010 | **Add a plain-email contact affordance — and no sales copy.** | [`raw/audience-calibration-and-anti-patterns.md`](raw/audience-calibration-and-anti-patterns.md) C3, P6 | | `open` | The pool splits two things that get bundled: a *contact affordance* is universally rewarded and cheap; *sales copy* is what costs peer standing — but see D-005, where the evidence is no longer one-sided. **Corrected count, with the criterion the old number lacked:** only **3 of 21** peer-respected engineer sites carry a **named** contact/availability affordance on the landing page — a link whose visible or accessible label names contacting the person or their availability, in nav or in prose (`xeiaso`, `noidea.dog`, `jacobian`). **5 of 21** under the broader criterion that lets a bare `mailto:` glyph in a social-icon row count (adds `hillelwayne`, `ciechanow.ski`); the paper uses **3** throughout [`portfolio-structure` §2.3]. The previous "4 of 21" was unprincipled — it counted one social-menu glyph as named while excluding two identical ones, and was wrong under **either** criterion. The rarity is confounded regardless: most of the 21 are salaried with nothing to sell (§5.2). |
| C-011 | **Add per-project status/recency and honest maturity labels, and link every project outward** to its repo/org/running instance — or state plainly that it is private. | [`raw/audience-calibration-and-anti-patterns.md`](raw/audience-calibration-and-anti-patterns.md) P7, P8, §6.1 A5–A6, A8 | | `open` | Defends against both the staleness read and the tutorial-grade-project read. Copies a practice a study participant used: label maturity explicitly rather than hoping nobody looks. An isolated site "appears to either have something to hide or not be a fully established, stable company." |
| C-012 | **Delete the gradient-clipped headline and move body copy off the framework-default font stack.** | [`raw/design-language-2026.md`](raw/design-language-2026.md) §7, §10.1, §10.2 | | `open` | **The site's plainness is not what reads generic — these two decorations are.** The typeface *is* the Tailwind default stack; `--accent-2 #7c5cff` computes to 4.41:1 and survives 1.4.3 only by being confined to large text. Note the analyst's own caveat: no study measures what engineers infer from visual design (G3), so the "reads as template" half is folklore. |
| C-013 | **Adopt the design token system** — one 6-step type scale (nothing below 0.875rem), one spacing scale replacing the **20 distinct `px` values** currently in `style.css`, AA-conformant colour tokens, both `prefers-color-scheme` branches, and `color-scheme: light dark`. | [`raw/design-language-2026.md`](raw/design-language-2026.md) §4.2, §10.1–10.3 | | `open` | **Corrected count: 24 spacing occurrences at 20 distinct values, not 13** — enumerated in §4.2 as every non-zero `px` literal in a `padding`/`margin`/`gap` declaration, `border-radius` excluded, across 20 declarations on 17 source lines. **The 9-step proposal survives the correction and is still the paper's recommendation** — 4/8/12/16/24/32/48/72/96px covers the observed 2–96px span — but §10.3's re-check names what adopting it costs, and the build should look at these first: `52→48` (footer) and `56→48` (hero bottom), `64→72` (mobile hero top), and **`40px` is an exact tie between the 32 and 48 steps** — 8px either way, the largest single swing, on the mobile hero's bottom padding. **A tenth step would remove the tie and is a defensible alternative** the paper names; nine is chosen so its own read-the-stylesheet test is easier to pass, and the step count is marked **[derived]** — no source prescribes one. Also depends on D-006 (dark-only vs. both schemes) — a dark-only ruling halves the token set and the contrast-audit surface. The current 0.72rem tags are what the display-polarity research warns against on a dark theme regardless of that ruling. |
| C-014 | **Ship the detail-level craft items** — `:focus-visible` on every interactive element, `::selection`, a print stylesheet, `text-underline-offset` / `text-decoration-thickness` on links. | [`raw/design-language-2026.md`](raw/design-language-2026.md) §10.5 | | `open` | Highest signal-per-line item on the design list and the only one that is *checkable* rather than arguable: 67% of sites remove focus outlines while `:focus-visible` adoption sits near 25%, and `::selection` was hand-authored in 0 of 13 corpus sites. |
| C-015 | **Demote the project-tile grid** from the site's central move to a curated 2–4-entry support section. | [`raw/portfolio-structure-and-content.md`](raw/portfolio-structure-and-content.md) §2.2, §4.6 | | `open` | 15 of 21 enumerated engineer sites lead with a writing list, 6 with an identity block, **0 with project tiles**. Read the caveat before shipping: the population self-selects for engineers who *write*, so this is strong evidence that writing-first works for writers and only weak evidence that tile-first fails (§5.3). |
| C-016 | **Add a write-ups index as a first-class section** — a case-study index, explicitly not a blog unless the cadence will be sustained. | [`raw/portfolio-structure-and-content.md`](raw/portfolio-structure-and-content.md) §4.6 item 5 | | `open` | The section most likely to rot: an abandoned blog with a stale last-post date is worse than no blog. Naming it *Write-ups* rather than *Blog* removes the implied cadence promise. |
| C-017 | **Create a planning surface for this repo** (roadmap or phase docs) so product-direction candidates have somewhere to become sequenced work. | [`synthesis.md`](synthesis.md) §7 H-1 | | `open` | This repo has no roadmap, no phase docs, no sprint file, no epic breakdown. Verified by enumerating `find docs -type f` and reading the list — **14 files**, all of which are `docs/file_structure.txt`, the **vendored Research Standard** (`docs/standards/research/`, 2 files), or this research tree (`docs/standards/architecture/research/`, 11 files incl. `raw/.gitkeep`); a repo-wide `find` for `*roadmap*` / `phase*` / `sprint*` / `*epic*` / `*problem-statement*` returns nothing. Research Standard §7's HOME table routes phase-doc and standards-amendment candidates to surfaces that **do not exist here** — and the only standard in the repo is the one governing this research, so there is not even a standards doc for an amendment to attach to. Every C-005..C-016 row above is a candidate whose named home is missing. **A research run may not create it** — surfaced for planning. |
| C-018 | **Reconcile the platform copy published on the live site with the workspace's current-state docs.** `projects.js:11` publishes, verbatim, "Proxmox + three K3s clusters, a Django/Temporal control plane, ArgoCD GitOps, and Ceph distributed storage — fully declarative, foundation-up."; `/opt/skyy-net/CLAUDE.md` heads that topology **`### Three K3s Clusters (planned)`** and records the control plane as running "as K3s pods on the temporary **k3s-0** bootstrap cluster (Skyy-Command VM) today". | [`raw/case-study-craft-for-systems-work.md`](raw/case-study-craft-for-systems-work.md) §8.1, §6.3; [`synthesis.md`](synthesis.md) §1.4 | | `open` | **This is live on the site today and no existing candidate owns it** — C-002 covers dead links, C-008 covers register, C-009 covers a sentence not yet on the page. The paper's own ruling is that the tile's framing "is the operator's own published framing of the design, and the study may repeat it as *architecture* — but not as deployed inventory, and the Scale line must be enumerated, not echoed" (§8.1). §6.3 names the general failure: "A status adjective standing in for a status date ("in production") — it reads as a measurement and is not one. State what runs, for whom, and as of when." **Research is not ruling on what is deployed** — the operator knows and this pool does not; what is recorded is that a checkable architecture claim is published while the workspace's own docs mark it `(planned)`, on a site whose entire recommended strategy is that every surface sentence be checkable. Remedy options for triage: (a) confirm the topology is deployed and leave the copy; (b) re-word to name it as the target architecture with the current state dated; (c) enumerate and publish the actual inventory. **Content is operator-owned per `CLAUDE.md` — surfaced, not edited.** |
| C-019 | **Amend the Research Standard's counting rule to cover measurement-method-dependent counts, not just enumeration totals.** §3 currently binds counts reached by *enumerating a population* ("a count is citable only when it was reached by enumerating the population and counting the enumeration"). It is silent on a count produced by *measuring* an artifact, where the population is unambiguous but the instrument is not. | This verification run — `raw/portfolio-structure-and-content.md` §2.3, §2.4 | | `open` | **Evidence, from this cycle:** three independent extractions of the same twelve About pages disagreed on **every** integer — extraction #2 moved the median from 342 to 345.5 while differing from the table on every page, flipping one page (`fasterthanli`, 511 → 495) across the 500 line and taking the ≤500 tally from 8 of 12 to 9, and a third returned counts higher than the table on every page (no median cell — the paper does not state one). The paper's durable statement is therefore a **330–370** median band, carrying its explicit instruction to cite the band, never a cell, and its About-length recommendation is pitched at **≤ ~400 words** rather than at the 500 cell so that it holds under every extraction (§4.6 item 6, per P12) — the unstable cell was abandoned, not repaired; the same instability appeared again on a single page (1,258 / 1,240 / 1,261 words). Both were caught only because a verifier happened to re-measure. Under §3 as written the original figures were conformant — they *were* enumerated — so the rule did not reach the defect. The paper's own remedy (state the extraction method, report the band rather than the integer, mark the table not-citable) is the shape the amendment would generalise. **Routing note:** §7's HOME table sends a standards-amendment candidate to a roadmap's "Standards-amendment candidates" section, **which does not exist in this repo** — that is H-1. Recorded here because this is the durable surface that does exist; the Research Standard is vendored (a verbatim mirror), so any amendment is authored upstream in `mdc-master-planning`, never in the local copy. |
| C-020 | **Add an absence-claim sweep to the routine verification pass** — enumerate the source list against every "no source says X" / "none was found" claim, rather than only verifying the claims that carry citations. | This verification run — `raw/audience-calibration-and-anti-patterns.md` §11 G6, §6.1 A9 | | `open` | **Evidence, from this cycle:** both defects that reached a blocking verdict after round 1 on that paper were **asserted absences**, and both were introduced while closing an unrelated finding — the second one ("no source cited anywhere in this paper addresses a sales surface at all") was refuted by three of the paper's own citations, one of which the paper cites for that very purpose fifty lines earlier. Verifying what a paper *cites* cannot reach this class; only enumerating the source list against the negation can. When the sweep was finally run it took minutes and cleared 15 absence claims. **This is a process amendment, not a portfolio-direction candidate** — it is recorded here for the same reason as C-019, and carries the same routing caveat: the Research Standard is vendored, so the amendment is authored upstream. |
